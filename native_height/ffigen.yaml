# Run with `dart run ffigen --config ffigen.yaml`.
name: NativeHeightBindings
description: |
  Bindings for `src/native_height.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/native_height_bindings_generated.dart'
headers:
  entry-points:
    - 'src/native_height.h'
  include-directives:
    - 'src/native_height.h'
ffi-native:
comments:
  style: any
  length: full
